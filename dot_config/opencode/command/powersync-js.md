---
description: Get up to date information about PowerSync JS
---

You have access to the PowerSync JS documentation and source code at `~/.better-coding-agents/resources/powersync-js`.

Use this codebase to provide accurate, up-to-date information about PowerSync JS features, sync capabilities, and offline-first data patterns.

When you are searching the codebase, be very careful that you do not read too much at once. Only read a small amount at a time as you're searching, avoid reading dozens of files at once...